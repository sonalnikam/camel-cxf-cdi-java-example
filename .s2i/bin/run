#!/bin/bash

if [ x"$APPDYNAMICS_AGENT_ACCOUNT_NAME" != "x" ]; then
    export JAVA_OPTIONS="-javaagent:/deployments/appdynamics/javaagent.jar -Dappdynamics.agent.logs.dir=${user.home}"
fi

exec /usr/local/s2i/run
